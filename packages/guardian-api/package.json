{
  "name": "@diana/guardian-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API do responsável (DIANA): serve o alerta estruturado — nunca a conversa integral (RF-16).",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js",
    "dev": "node --watch dist/main.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@diana/contracts": "*",
    "@fastify/cors": "^9.0.1",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.8.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
